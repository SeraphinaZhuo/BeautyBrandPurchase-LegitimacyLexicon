# METHODS.md — 方法说明书
**BPLL 项目 | 版本 v1.1 | 2026-07-28**

本文档汇总项目的全部方法规则。Research Brief 负责“为什么做、检验什么”；本文档负责“具体怎么做、按什么标准”。两份文档随仓库开源。

---

## 1. 分析原则

本项目在正式分析前固定以下方法原则。后续如需修改，应更新版本号，并在版本历史中说明变更内容、原因与日期，以便追踪方法变化。

1. **分析单位是发言，不是用户**  
   本项目只分析公开文本中的“发言”及其话语特征，不推断具体用户的心理、身份或行为变化。因此，报告中不使用“某类消费者流失了”之类的个体层面因果表述；允许使用的表述是“某类发言的占比发生了变化”或“某类话语在特定时段中更常见”。

2. **结论只适用于公开发言人群**  
   语料来自关键词检索，因此只覆盖在平台上公开发声、且发言被关键词命中的人群。没有发声的人、未被关键词覆盖的讨论、私域讨论与线下态度，均不在本项目数据范围内。若要推断沉默人群或总体消费者态度，需要另行设计问卷或访谈研究。

3. **词典需通过验证后才进入统计分析**  
   词典自动标签不会直接用于正式结论。每一类词典都需要与人工标注结果对照，并公开报告 precision / recall 等指标。只有达到 §6 所列标准的类别，才进入自动化占比统计；未达标类别只用于质性阅读和案例引用。

4. **使用中性术语**  
   分析文本避免使用带有明显价值判断的修辞，例如“社交剥削”。本项目统一使用较中性的概念表述，包括“价值认同型”“社会信号型”“信任价值”“信号价值”等。

5. **普通用户与机构账号分开分析**  
   普通用户构成主样本。当前脚本将 `user_type` 为“蓝V认证账号”或“品牌官方”的记录分流为机构样本，主要用于分析品牌回应、媒体传播或机构表态，不与普通用户发言混合计算。

6. **公开代码与聚合结果，不默认公开原文级数据**  
   本项目开源代码、词典、清洗规则与聚合统计结果。原文级数据是否公开，取决于平台服务条款、隐私风险与研究伦理要求；默认不公开可追溯到单条发言的原文数据。

7. **正式结果在方法固定后再运行**  
   涉及 T0 → T1 → T2 真实对比的统计分析，只在 Research Brief 冻结、词典完成验证之后执行。在此之前，管线开发仅使用测试数据或不会暴露真实前后变化的输入方式，以避免“先看结果、再调整方法”的问题。具体做法见 §7。


## 2. 数据清洗规则

微博原始数据共 **31,677 条**。数据清洗由 `clean_weibo.py` 执行；清洗后得到普通用户主样本 **5,370 条**。清洗过程遵循“移动而非删除”的原则：原始文件保持只读，被排除的记录分别写入归档文件，并保留 `exclusion_rule` 字段，以便复查、恢复和重新运行。机构账号不与普通用户主样本混合计算。

脚本按以下顺序执行清洗与分流：文本规范化 → R1 去重 → R6 品牌词误命中 → R4 账号分流 → R2 广告识别 → R3 低信息过滤 → R8–R12 残余噪音过滤 → R7 分析标记。R2、R3 与 R8–R12 仅作用于普通用户记录。

* **文本规范化**<br>
  脚本先对 HTML 实体进行还原，并合并连续空白字符，生成 `text_norm`。规范化文本用于去重和后续规则判断；表情、话题标签等内容不在此阶段直接删除。<br>

* **R1 去重**<br>
  若 `post_id + text_norm` 完全相同，只保留首次出现的记录，其余记录移入 `archive_duplicates.csv`。该规则针对采集过程产生的完全重复，不将用户在不同帖子中的相似表达视为重复。<br>

* **R6 品牌词误命中**<br>
  该规则在账号分流和广告识别之前执行。对于“花王”样本，若文本出现“牡丹”“芍药”“花中之王”“工笔”“书法”“红楼”等花卉、艺术或文学语境，同时没有卫生巾、洗护、清洁用品、碧柔、乐而雅等日化产品语境，则判定为品牌关键词误命中，移入 `archive_offtopic.csv`。<br>

* **R4 账号分流**<br>
  `user_type` 为“蓝V认证账号”或“品牌官方”的记录进入 `sample_institutional.csv`，不进入普通用户主样本。机构样本在分流后完整保留，不再执行 R2、R3 与 R8–R12，以避免把新闻报道、品牌公告或带有价格和活动信息的机构内容误判为普通用户广告或噪音。<br>

* **R2 广告、带货与抽奖内容**<br>
  广告识别采用分层打分，而不是单一关键词删除。文本命中任一强信号，例如“领券”“拼团价”“抽奖平台”“打款”“到手价”“私我”“微店”“下单链接”等，即判定为广告。若只命中价格、大促、库存、直播间、旗舰店、包邮、折扣、福利、安利等弱信号，则必须满足以下任一条件才会移出：命中至少两个不同弱信号；或命中至少一个弱信号，同时文本前 30 字在至少 3 条记录中重复出现。被识别为广告的记录移入 `archive_promo.csv`。其中同时包含核污水相关词的记录通过 `AUDIT_promo_rows_containing_crisis_words` 单独计数，供人工复核。<br>

* **R3 低信息内容**<br>
  脚本不使用一般性的短文本阈值，因为“不买”“抵制”等短句仍可能表达明确态度。计算有效长度时，会剔除网址、话题标签、@提及、平台表情、emoji、空白和标点。只有有效长度不超过 1 个字符，或全文命中已知噪音表达时，记录才会移入 `archive_lowinfo.csv`。噪音表达包括“转发微博”“图片评论”“关注”“收藏”“蹲”“666”等无可分析信息的占位内容。<br>

* **R8 新闻线稿式转发**<br>
  以 `【#话题#` 开头、呈现微博新闻标题模板的普通用户记录，被视为缺少个人立场的新闻线稿式转发，移入 `archive_residual_noise.csv`，标记为 `R8_newswire_repost`。即使文本包含危机词，该规则仍然执行。<br>

* **R9 图像或视频依赖内容**<br>
  若文本有效长度不超过 15 个字符，且主要依赖“图一”“最后一张”“看图”“视频里”等当前语料未包含的视觉信息，则标记为 `R9_image_dependent`。较长的产品评价不会仅因提到图片而被排除。包含核污水相关词的记录不适用本规则。<br>

* **R10 抽奖参与或排队式回复**<br>
  若文本有效长度不超过 15 个字符，且内容主要是“扣1”“蹲一个”“求扣”等参与抽奖或排队的回复，则标记为 `R10_giveaway_participation`。该规则针对参与者回复，与 R2 中发布广告或抽奖机制的账号相区分。包含核污水相关词的记录不适用本规则。<br>

* **R11 话题堆叠型推广文案**<br>
  若一条记录包含至少 3 个话题标签，并同时出现“推荐”“必收”“种草”“好物”“清单”“安利”“榜单”“测评”“囤”或“入手”等商业提示词，则标记为 `R11_hashtag_promo`。包含核污水相关词的记录不适用本规则。<br>

* **R12 缺少上文的回复碎片**<br>
  若文本有效长度不超过 8 个字符，且没有出现危机、品牌、产品功效、购买、价格、替代等主题词，则视为语义依赖缺失父评论的回复碎片，标记为 `R12_reply_fragment`。包含核污水相关词的记录不适用本规则。<br>

* **R7 分析标记**<br>
  通过清洗的普通用户记录以及机构样本都会增加分析标记，但不会因此被移出。`crisis_flag` 表示文本包含“核污水”“核废水”“核污染”“排海”“核辐射”“辐射”或“福岛”等词；`huaxizi_flag` 表示蜂花样本中同时出现花西子、李佳琦、眉笔或“79元”等相关表达；`guohuo_flag` 表示文本包含“国货”“国产”或“国牌”。<br>

* **R5 原始数据只读与结果可复现**<br>
  清洗脚本不修改原始文件，不调用外部服务，也不包含随机过程。同一输入应产生同一输出。运行结果及各规则移出数量写入 `dq_stats.json`，可通过文件差异检查复现一致性；汇总报告写入 `dq_report.md`。<br>

## 3. 测量方法：话语类别占比

本项目不使用通用情绪分数作为核心指标。原因是，在本语料中，文本情绪和品牌态度经常不一致。例如，“干得漂亮”在字面上可能是正面表达，但在具体语境中可能是在支持抵制；“好用到纠结”字面上带有负面情绪，但可能表达的是对产品的认可。评论文本又普遍较短，中位数约为 13 字，因此通用情绪模型更容易误判。

因此，本项目采用“话语类别占比”作为主要指标。

* **核心指标**<br>
  在每一个“品牌 × 时段”的分析单元中，计算某一话语类别出现的发言数，占该单元全部发言数的比例。<br>
  例如，可以计算某品牌在 T1 时段中，“安全恐慌”类发言占全部发言的比例。<br>

* **类别体系**<br>
  话语类别分为两个层级。<br>
  第一层区分“价值认同型”和“社会信号型”话语。<br>
  第二层进一步区分七类危机反应：安全恐慌、立场抵制、理性辩护、摆烂虚无、割舍挣扎、替代转投和国货红利。<br>
  各类别的具体定义见编码手册。

* **允许多标签**<br>
  一条发言可以同时属于多个类别。<br>
  例如，一条评论可能既表达“安全恐慌”，也表达“替代转投”。因此，各类别占比是分别计算的，不要求加总为 100%。

* **置信区间**<br>
  每个占比都会报告 Wilson 95% 置信区间。<br>
  在最小分析单元中，样本量约为 129 条发言，对应的置信区间半宽约为 ±6–8 个百分点。因此，本项目更关注方向性差异和趋势变化，而不是过度解读小幅度的点估计差异。

* **稳健性检查**<br>
  除主分析外，本项目还提供两类补充结果。<br>
  第一类是点赞加权版本，用来观察高互动内容是否明显改变总体话语结构。<br>
  第二类是主贴与评论分开统计的版本，用来区分原始发帖和互动回应中的话语差异。<br>
  这些结果作为附录或补充输出提供。

* **与研究问题的对应关系**<br>
  M1 关注“立场抵制”占比在不同品牌之间的变化轨迹。<br>
  M2 关注“安全恐慌”占比在不同品牌之间的变化轨迹。<br>
  二者的相对强度用“立场抵制占比变化量 − 安全恐慌占比变化量”表示，即 Δ立场 − Δ恐慌。<br>
  机制 C 关注各品牌在 T0 阶段的第一层话语构成，是否与 T1 阶段的危机反应结构存在对应关系。由于品牌层面的样本量只有 5，该部分只做描述性展示，不做强推断。<br>
  机制 D 关注“理性辩护”类发言在互动结构中的位置。具体做法是查看这类发言挂靠在哪些 `parent_post_id` 下，以及它们引发了怎样的回应。

## 4. 种子词准入标准

本项目的词典不是直接由高频词自动生成，而是先根据人工精读和编码手册形成候选词，再用脚本检查这些词在语料中的实际出现情况。

具体顺序如下：

1. 精读部分语料，并在编码手册中记录类别判断；
2. 根据类别定义和精读结果，人工筛选候选种子词；
3. 使用 `audit_seed_words.py` 检查每个词在主语料中的出现频次；
4. 对低频或未出现的词进行公开标记，而不是悄悄删除。

这种顺序的目的，是避免把“出现频率高”误当成“类别判别力强”。也就是说，词典首先服务于类别定义，其次才通过语料频次进行检查。

一个词进入种子表，需要满足以下标准：

* **在语料中有根据**<br>
  优先收录在精读语料中真实出现过的表达。<br>
  如果某些词在后续频次检查中没有出现，或出现次数很少，不会直接删除，但会在词典中标记出来。

* **不是个体独特表达**<br>
  种子词应当在多个发言者、多个语境中出现。<br>
  只属于某一个用户的特殊说法，不进入种子词表。

* **优先保证判别力**<br>
  种子词阶段宁可少收，也不为了提高覆盖率而纳入含义模糊的词。<br>
  覆盖率不足的问题，后续通过词向量扩展和人工验证来处理。

* **优先使用自然表达**<br>
  词典尽量收录用户实际使用的说法，而不是研究者自己发明的分析术语。<br>
  例如，用户常说的“避雷”“不敢买”“换国货”，通常比抽象概念词更适合作为词典条目。

`audit_seed_words.py` 会自动检查每个种子词在清洗后微博主样本和小红书语料中的出现频次：

* 频次为 0 的词，标记为 `corpus_absent`；
* 频次为 1–4 的词，标记为 `sparse`；
* `corpus_absent` 和 `sparse` 合并视为 `prior_driven`。

`prior_driven` 词项会保留在最终词表中，并在词典文件里公开标记。<br>
这类词的作用主要是补足理论类别的覆盖范围，尤其是对危机语料中本来就较少出现的“社会信号型”表达。它们不被视为强语料证据。后续词典验证时，这些词是否真正适合对应类别，将由人工标注结果统一判断。

## 5. 消歧规则

有些词在不同语境下含义不同，不能只靠词本身判断类别。例如，“避雷”可能是在说产品不好用，也可能是在说因为核污水问题而避开日货。为减少误判，本项目对部分高风险词设置了上下文规则。

这些规则在 `tag_and_aggregate.py` 中执行。未列入下表的词，按照种子表中的 `layer` 和 `category` 直接归类。

| 词        | 默认归属         | 何时改判                              | 改判后归属   |
| -------- | ------------ | --------------------------------- | ------- |
| 避雷 / 踩雷  | L1 功效型（质量负评） | 上下文含“核”“辐射”“排海”“日货”“日系”“清单”等词     | L2 安全恐慌 |
| 烂脸       | L1 功效型       | 上下文同时出现“还买”“谁买”“活该”“应该”等表达，并且指向日货 | L2 立场抵制 |
| 囤货       | 不计入          | 上下文含“批次”“生产日期”“核”等词               | L2 安全恐慌 |
| 焦虑       | L2 安全恐慌      | 上下文含“别”“不要”“没必要”“制造”等否定焦虑的表达      | L2 理性辩护 |
| 检测       | L2 理性辩护      | 上下文含“检测仪”“自己测”或“买……测”等表达          | L2 安全恐慌 |
| 本子       | 不计入          | 上下文含“日本”“日货”“抵制”等词                | L2 立场抵制 |
| 随便 / 无所谓 | 不计入          | 上下文含“辐射”“核”“反正”“摆烂”等词             | L2 摆烂虚无 |

这张表会在词典 v1 定稿时固定。之后如果新增或修改规则，需要在版本历史中说明原因。

## 6. 词典验证标准

词典进入正式分析前，需要通过人工标注验证。验证的重点不是证明词典“完全正确”，而是判断每一类词典是否足够可靠，能否用于自动统计。

* **人工标注规模**<br>
  人工标注分为试标和正式标注两个阶段。试标共进行两轮，每轮 50 条；正式样本共 850 条，分为 300 条、300 条和 250 条三个批次。正式批次由 3 名标注者独立完成。标注一致性使用 Fleiss' κ 逐类别报告。<br>

* **试标一致性与定义修订**<br>
  两轮试标用于检验编码手册是否能够被稳定执行，并据此澄清边界案例。五个核心态度标签——价值认同型、安全恐慌、立场抵制、割舍挣扎和国货红利——的平均一致性由第一轮的 **0.337** 提升至第二轮的 **0.685**，约为原来的 2.03 倍，按本项目采用的解释区间由“较差”进入“好”。其中值得注意的是，“立场抵制”由 **0.145** 提升至 **0.760**，而它在第一轮是所有标签中一致性最低的一项，第二轮则升至所有标签中的第二高。需要声明的是，试标结果只用于修订和冻结定义，不进入最终人工标准。<br>

* **类别准入标准**<br>
  每一类词典单独评估 precision。只有当某一类别的 precision ≥ 0.75 时，该类别才进入自动化占比统计。若某一类别未达到这一标准，则降级为“人工辅助类别”，仍可用于质性阅读、案例筛选和引用，但不输出该类别的自动化占比统计。<br>

* **覆盖率诊断**<br>
  本项目会报告主样本中至少命中一个 L2 标签的发言占比，用来观察词典覆盖了多少危机反应表达。该指标不设置硬性门槛；但如果覆盖率低于 30%，报告中需要专门讨论可能的漏检来源，例如隐喻、反讽、缩写、平台黑话或新出现的流行表达。<br>

* **precision 与 recall 的使用方式**<br>
  precision 用于判断某一类别能否进入自动统计；recall 用于判断词典是否漏掉了大量相关表达。因此，precision 直接关系到类别是否“准许上线”，recall 主要用于解释结果边界和后续改进方向。<br>

## 7. 正式运行规则

本节说明正式统计分析何时运行，以及如何记录运行过程。目的不是限制复现，而是避免在方法尚未固定时提前查看真实结果，从而影响后续规则设定。

- **默认不运行真实前后对比**  
  分析脚本默认不接受完整的 T0 → T1 → T2 真实数据输入。若要运行正式分析，必须显式加入 `--unseal` 参数。这样做是为了把开发测试和正式统计区分开。

- **开发阶段只使用测试输入**  
  在方法冻结前，管线开发使用两类测试输入：

  1. `--placebo shuffle`：随机打乱品牌标签，用于检查脚本是否能正常完成清洗、打标和汇总。随机种子固定为 42，便于复现。
  2. `--placebo t0-only`：只输入 T0 单一时段数据，用于检查单期统计结果是否正常生成，但不查看危机前后的变化。

- **正式运行会留下记录**  
  每次运行都会写入 `run_log.jsonl`，记录运行时间、运行模式、输入文件摘要哈希等信息。该日志用于说明哪些结果是在方法冻结前生成的测试结果，哪些是在方法冻结后生成的正式结果。

- **结果解释以正式运行为准**  
  测试输入产生的结果只用于检查代码和输出格式，不用于 substantive interpretation。正式报告中的统计结论，只基于通过 `--unseal` 执行、且写入运行日志的正式分析结果。

## 8. 人工标注流程

本节说明词典验证所需的人工标注如何组织，包括样本抽取、两轮试标、正式批次安排和分歧处理。

* **语料基础与标注规模**<br>
  * 微博原始数据共 **31,677 条**；经 `clean_weibo.py` 清洗和账号分流后，普通用户主样本为 **5,370 条**。
  * 试标进行两轮，每轮 **50 条**。两轮试标用于检验并修订标签定义，不计入最终人工标准。
  * 正式样本共 **850 条**，依次拆分为 **300 条、300 条和 250 条**，共三个批次，以便分阶段组织标注、检查文件完整性和控制标注负担。

* **标注样本的抽取方式**<br>
  * 标注样本从清洗后的普通用户主样本中抽取。抽样不采用完全随机方式，因为危机相关发言在全量主样本中的占比较低，完全随机抽样可能不足以评估各危机反应类别。
  * 每个批次的一半从 `crisis_flag = 1` 的发言中随机抽取；另一半从其余发言中，按照“品牌 × 时段”的实际分布进行分层抽取。
  * 正式样本与试标样本分开管理，试标标注不进入最终人工标准。三个正式批次之间不得重复。
  * 这种配额设计有意提高危机相关内容的比例，适合评估标签和词典的 precision / recall，**不适合**估计各类话语在总体语料中的真实占比。总体占比只能由词典对 5,370 条普通用户主样本的全量打标结果估计。
  * 抽样由 `sample_for_annotation.py` 执行，随机种子固定；批次编号、抽样条件和样本去重结果应随运行记录保存。

* **标注者安排**<br>
  * 三个正式批次均由 3 名标注者独立标注。
  * 标注者只能看到发言文本和匿名编号，看不到品牌、时段等元数据。三名标注者接收相同条目，但每人的条目顺序独立打乱，以减少顺序效应造成的一致性虚高。
  * 标注者不被告知研究假设；正式标注期间，标注者之间不讨论具体条目。

* **两轮试标与定义冻结**<br>
  * 第一轮试标完成后，标注者集中核对分歧，重点识别标签边界、排除条件和容易混淆的表达，并据此修订编码手册和标注指南。
  * 第二轮试标用于检验修订后的规则能否提高一致性。五个核心态度标签的平均 Fleiss' κ 从 **0.337** 提升至 **0.685**；“立场抵制”从 **0.145** 提升至 **0.760**，由第一轮一致性最低的标签升至第二轮第二高。
  * 第二轮试标和讨论结束后，标签定义冻结。正式标注开始后，不再根据正式样本的结果修改标签定义；如确需变更，必须停止当前正式标注、更新版本记录并重新开始受影响批次。

* **最终标准的形成与分歧处理**<br>
  * 正式批次中，一条发言的某个标签以多数意见为准，即 3 名标注者中至少 2 人作出相同判断。
  * 若多数意见为“无法判断”，该标签不进入最终人工标准；相关条目数量与原因在验证报告中如实报告。
  * 标注一致性使用 Fleiss' κ 逐类别报告。标注者是否纳入一致性计算，只依据预先规定的质量标准，不依据其具体标注倾向或是否符合研究预期。

## 9. 版本历史

* **v1.1（2026-07-28）**：依据 `clean_weibo.py` 的实际逻辑重写清洗规则，补充 R8–R12 及危机词保护条件；更新微博数据规模为原始 31,677 条、普通用户主样本 5,370 条；将人工标注方案更新为两轮各 50 条试标和 850 条正式样本（300／300／250）；补充两轮试标的一致性改善结果。
* **v1.0（2026-07-08）**：发布方法说明书初版。
